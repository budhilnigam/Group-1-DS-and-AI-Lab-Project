[
  {
    "repo": "django/django",
    "file": "tests/forms_tests/field_tests/test_multiplechoicefield.py",
    "chunk_id": "PR20998_2995226994",
    "diff": "@@ -91,5 +91,5 @@ def test_validate_duplicated_values(self):\n \n     def test_validate_duplicated_invalid_values(self):\n         f = MultipleChoiceField(choices=[(\"1\", \"one\"), (\"2\", \"Two\")])\n-        with self.assertRaises(ValidationError):\n-            f.validate([\"1\", \"1\", \"invalid\", \"invalid\"])\n+        with self.assertRaisesMessage(ValidationError, \"invalid-one\"):\n+            f.validate([\"1\", \"1\", \"invalid-one\", \"invalid-two\", \"invalid_two\"])",
    "violations": [
      {
        "type": "",
        "line": 95,
        "review_comment": "This test no longer tests duplicated values. I wonder if we should omit the \"1\"s, which are valid, in favor of duplicate invalid values, but still add a separate invalid value to ensure we're testing for message determinism."
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/postgres_tests/test_operations.py",
    "chunk_id": "PR20964_2973333194",
    "diff": "@@ -370,6 +371,21 @@ def test_drop_nonexistent_extension(self):\n         self.assertEqual(len(captured_queries), 1)\n         self.assertIn(\"SELECT\", captured_queries[0][\"sql\"])\n \n+    def test_create_extension_skipped_on_non_postgresql(self):\n+        operation = CreateExtension(\"tablefunc\")\n+        project_state = ProjectState()\n+        new_state = project_state.clone()\n+        with connection.schema_editor(atomic=False) as editor:\n+            with mock.patch.object(editor.connection, \"vendor\", \"sqlite\"):",
    "violations": [
      {
        "type": "",
        "line": 379,
        "review_comment": "> I\u2019m unsure which release version of the docs to target\r\n\r\nDocs are only required for regression fixes in minor releases and for new features in major releases"
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/postgres_tests/test_operations.py",
    "chunk_id": "PR20964_2976953625",
    "diff": "@@ -370,6 +371,21 @@ def test_drop_nonexistent_extension(self):\n         self.assertEqual(len(captured_queries), 1)\n         self.assertIn(\"SELECT\", captured_queries[0][\"sql\"])\n \n+    def test_create_extension_skipped_on_non_postgresql(self):\n+        operation = CreateExtension(\"tablefunc\")\n+        project_state = ProjectState()\n+        new_state = project_state.clone()\n+        with connection.schema_editor(atomic=False) as editor:\n+            with mock.patch.object(editor.connection, \"vendor\", \"sqlite\"):",
    "violations": [
      {
        "type": "",
        "line": 379,
        "review_comment": "@cliffordgama This situation is tricky, since all of the postgres-specific tests are already skipped on all other backends. So I'm not certain it's even worth adding a test for this cleanup. wdyt?"
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/postgres_tests/test_operations.py",
    "chunk_id": "PR20964_2976965127",
    "diff": "@@ -370,6 +371,21 @@ def test_drop_nonexistent_extension(self):\n         self.assertEqual(len(captured_queries), 1)\n         self.assertIn(\"SELECT\", captured_queries[0][\"sql\"])\n \n+    def test_create_extension_skipped_on_non_postgresql(self):\n+        operation = CreateExtension(\"tablefunc\")\n+        project_state = ProjectState()\n+        new_state = project_state.clone()\n+        with connection.schema_editor(atomic=False) as editor:\n+            with mock.patch.object(editor.connection, \"vendor\", \"sqlite\"):",
    "violations": [
      {
        "type": "",
        "line": 379,
        "review_comment": "@jacobtylerwalls that's fine by me \ud83d\udc4d "
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/postgres_tests/test_operations.py",
    "chunk_id": "PR20964_2977135153",
    "diff": "@@ -370,6 +371,21 @@ def test_drop_nonexistent_extension(self):\n         self.assertEqual(len(captured_queries), 1)\n         self.assertIn(\"SELECT\", captured_queries[0][\"sql\"])\n \n+    def test_create_extension_skipped_on_non_postgresql(self):\n+        operation = CreateExtension(\"tablefunc\")\n+        project_state = ProjectState()\n+        new_state = project_state.clone()\n+        with connection.schema_editor(atomic=False) as editor:\n+            with mock.patch.object(editor.connection, \"vendor\", \"sqlite\"):",
    "violations": [
      {
        "type": "",
        "line": 379,
        "review_comment": "@varunkasyap let's just remove the test \ud83d\udc4d "
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "django/forms/fields.py",
    "chunk_id": "PR20960_2973756353",
    "diff": "@@ -965,7 +965,7 @@ def validate(self, value):\n         if self.required and not value:\n             raise ValidationError(self.error_messages[\"required\"], code=\"required\")\n         # Validate that each value in the value list is in self.choices.\n-        for val in value:\n+        for val in set(value):",
    "violations": [
      {
        "type": "",
        "line": 969,
        "review_comment": "Suggestion: Include a comment here as to why `set` is being used."
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/forms_tests/field_tests/test_multiplechoicefield.py",
    "chunk_id": "PR20960_2983720167",
    "diff": "@@ -76,3 +76,20 @@ def test_multiplechoicefield_changed(self):\n     def test_disabled_has_changed(self):\n         f = MultipleChoiceField(choices=[(\"1\", \"One\"), (\"2\", \"Two\")], disabled=True)\n         self.assertIs(f.has_changed(\"x\", \"y\"), False)\n+\n+    def test_validate_duplicated_values(self):\n+        f = MultipleChoiceField(\n+            choices=[\n+                (\"1\", \"One\"),\n+                (\"2\", \"Two\"),\n+                (\"3\", \"Three\"),\n+                (\"4\", \"Four\"),\n+                (\"5\", \"Five\"),\n+            ]\n+        )\n+        self.assertIsNone(f.validate([\"4\", \"4\", \"5\", \"5\"]))\n+\n+    def test_validate_duplicated_invalid_value(self):",
    "violations": [
      {
        "type": "",
        "line": 92,
        "review_comment": "The test name primes me to expect duplicate invalid values, but you only have one invalid value."
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/db_functions/test_uuid.py",
    "chunk_id": "PR20890_2913556259",
    "diff": "@@ -38,17 +38,22 @@ def test_uuid7_shift(self):\n         past = datetime(2005, 11, 16, tzinfo=timezone.utc)\n         shift = past - now\n         m = UUIDModel.objects.create(uuid=UUID7(shift))\n-        self.assertTrue(str(m.uuid).startswith(\"0107965e-e40\"), m.uuid)\n+        m.refresh_from_db()",
    "violations": [
      {
        "type": "",
        "line": 41,
        "review_comment": "I'm surprised `refresh_from_db` is necessary here as `supports_uuid7_function_shift` is only supported on Postgres and we use `RETURNING` on the insert for expressions (`create` delegates to `Model.save`). Are you sure this is the case?\n\nWe could be future proof with\n\n```suggestion\n        if connection.features.can_return_columns_from_insert:\n            m.refresh_from_db()\n```\n\nbut today there is no intersection with `supports_uuid7_function_shift and not can_return_columns_from_insert`."
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/db_functions/test_uuid.py",
    "chunk_id": "PR20890_2913607114",
    "diff": "@@ -38,17 +38,22 @@ def test_uuid7_shift(self):\n         past = datetime(2005, 11, 16, tzinfo=timezone.utc)\n         shift = past - now",
    "violations": [
      {
        "type": "",
        "line": 39,
        "review_comment": "Could we simply use a fixed `shift` instead?\n\nAs pointed out in the other PR Python-now and Database-now will risking shifting so we want to truly test things out here we should extract the database level `Now()` at insertion time and generate a UUIDv7 from it.\n\nAn alternative could we to do something like\n\n```python\nshift = timedelta(minutes=1)\nm = UUIDModel.objects.create(uuid=UUID7(shift))\nuuid_timestamp = datetime.fromtimestamp(\n    self.uuid.time / 1000\n)\nself.assertGreaterEqual(datetime.now(timezone.utc) - uuid_timestamp, shift)\n```"
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/db_functions/test_uuid.py",
    "chunk_id": "PR20890_2927546566",
    "diff": "@@ -38,17 +38,22 @@ def test_uuid7_shift(self):\n         past = datetime(2005, 11, 16, tzinfo=timezone.utc)\n         shift = past - now",
    "violations": [
      {
        "type": "",
        "line": 39,
        "review_comment": "Nice, I like getting rid of the `startswith()` call on fixed values.\n\nI would also consider something like this:\n```py\nself.assertAlmostEqual(ts1.total_seconds(), ts2.total_seconds(), places=1)\n```\n\n... but I'm also fine with your last proposal @felixxm."
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/db_functions/comparison/test_coalesce.py",
    "chunk_id": "PR20864_2931792899",
    "diff": "@@ -75,3 +78,16 @@ def test_empty_queryset(self):\n             with self.subTest(description), self.assertNumQueries(1):\n                 qs = Author.objects.annotate(annotation=Coalesce(empty_query, 42))\n                 self.assertEqual(qs.first().annotation, 42)\n+\n+    @skipUnless(\n+        connection.vendor == \"oracle\",\n+        \"Oracle specific test for Coalesce with JSONField\",\n+    )\n+    def test_json_coalesce_with_char_oracle_specific(self):\n+        \"\"\"Test Coalesce with JSONField output_field on Oracle database.\"\"\"",
    "violations": [
      {
        "type": "",
        "line": 87,
        "review_comment": "One of Django's conventions is to only add a docstring for a test if it communicates more information than the test method name. And to avoid \"Test/Ensure...\" since all tests test things. So you can likely chop this."
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/db_functions/comparison/test_coalesce.py",
    "chunk_id": "PR20864_2931825415",
    "diff": "@@ -75,3 +78,16 @@ def test_empty_queryset(self):\n             with self.subTest(description), self.assertNumQueries(1):\n                 qs = Author.objects.annotate(annotation=Coalesce(empty_query, 42))\n                 self.assertEqual(qs.first().annotation, 42)\n+\n+    @skipUnless(\n+        connection.vendor == \"oracle\",\n+        \"Oracle specific test for Coalesce with JSONField\",\n+    )\n+    def test_json_coalesce_with_char_oracle_specific(self):\n+        \"\"\"Test Coalesce with JSONField output_field on Oracle database.\"\"\"\n+        CoalesceJSONTestModel.objects.create(summary=None, metadata={\"text\": \"test\"})\n+        # mixed JSON and CharField wrapped",
    "violations": [
      {
        "type": "",
        "line": 89,
        "review_comment": "I see this comment is styled after `test_mixed_values()`. How about placing your new test directly after that one?"
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/db_functions/models.py",
    "chunk_id": "PR20864_2932020803",
    "diff": "@@ -60,3 +60,8 @@ class FloatModel(models.Model):\n class UUIDModel(models.Model):\n     uuid = models.UUIDField(null=True)\n     shift = models.DurationField(null=True)\n+\n+\n+class CoalesceJSONTestModel(models.Model):",
    "violations": [
      {
        "type": "",
        "line": 65,
        "review_comment": "We're going to do our best to reuse models for future tests, so I'd use a more generic name like \"JSONModel\" (like the others)."
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "django/db/models/functions/comparison.py",
    "chunk_id": "PR20864_2932331089",
    "diff": "@@ -86,9 +86,13 @@ def empty_result_set_value(self):\n         return None\n \n     def as_oracle(self, compiler, connection, **extra_context):\n-        # Oracle prohibits mixing TextField (NCLOB) and CharField (NVARCHAR2),\n+        # Oracle prohibits mixing TextField,\n+        # JSONField (NCLOB) and CharField (NVARCHAR2),\n         # so convert all fields to NCLOB when that type is expected.\n-        if self.output_field.get_internal_type() == \"TextField\":\n+        if (\n+            self.output_field.get_internal_type() == \"TextField\"\n+            or self.output_field.get_internal_type() == \"JSONField\"\n+        ):",
    "violations": [
      {
        "type": "",
        "line": 95,
        "review_comment": "Maybe\n```suggestion\n        if self.output_field.get_internal_type() in (\"TextField\", \"JSONField\"):\n```"
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/db_functions/comparison/test_coalesce.py",
    "chunk_id": "PR20864_2932374547",
    "diff": "@@ -75,3 +78,16 @@ def test_empty_queryset(self):\n             with self.subTest(description), self.assertNumQueries(1):\n                 qs = Author.objects.annotate(annotation=Coalesce(empty_query, 42))\n                 self.assertEqual(qs.first().annotation, 42)\n+\n+    @skipUnless(\n+        connection.vendor == \"oracle\",\n+        \"Oracle specific test for Coalesce with JSONField\",\n+    )",
    "violations": [
      {
        "type": "",
        "line": 85,
        "review_comment": "Seeing this skip here challenged me to reconsider what behavior we actually want to support here. (It's an unusual bug report in that it didn't come from a user: it came from a tangent during another code review.)\n\nI think postgres is doing the right thing by complaining about mixed char and json types. (TIL that MySQL and SQLite are so loose with types!)\n\nSo, if we're going to build in some extra type-smoothing into `as_oracle()`, then we should only do it for a legitimate use case that also works on postgres.\n\nI think a use case for mixing json and char would be for json-parsable-strings that are stored in a CharField. If you `Cast()` the json-parsable primitive, it should work, and does work on Postgres. But for Oracle you hit this condition where varchar and NCLOB are mixed, and so we end up needing the code change in `as_oracle()`.\n\nI rejiggered the test case like this:\n```diff\ndiff --git a/tests/db_functions/comparison/test_coalesce.py b/tests/db_functions/comparison/test_coalesce.py\nindex 776321b337..f0ec06cd8a 100644\n--- a/tests/db_functions/comparison/test_coalesce.py\n+++ b/tests/db_functions/comparison/test_coalesce.py\n@@ -79,15 +79,12 @@ class CoalesceTests(TestCase):\n                 qs = Author.objects.annotate(annotation=Coalesce(empty_query, 42))\n                 self.assertEqual(qs.first().annotation, 42)\n \n-    @skipUnless(\n-        connection.vendor == \"oracle\",\n-        \"Oracle specific test for Coalesce with JSONField\",\n-    )\n+    @skipUnlessDBFeature(\"supports_primitives_in_json_field\")\n     def test_json_coalesce_with_char_oracle_specific(self):\n         \"\"\"Test Coalesce with JSONField output_field on Oracle database.\"\"\"\n-        CoalesceJSONTestModel.objects.create(summary=None, metadata={\"text\": \"test\"})\n+        CoalesceJSONTestModel.objects.create(summary=\"1\", metadata={\"text\": \"test\"})\n         # mixed JSON and CharField wrapped\n         result = CoalesceJSONTestModel.objects.annotate(\n-            coalesced_value=Coalesce(\"summary\", \"metadata\", output_field=JSONField())\n+            coalesced_value=Coalesce(Cast(\"summary\", JSONField()), \"metadata\")\n         ).first()\n-        self.assertEqual(result.coalesced_value, {\"text\": \"test\"})\n+        self.assertEqual(result.coalesced_value, 1)\n```\n\nCan you pursue this a little bit and see if I'm right about that? \ud83d\ude4f \n\nFeel free to adjust and use better values than \"1\" \ud83d\ude04"
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/db_functions/comparison/test_coalesce.py",
    "chunk_id": "PR20864_2935126086",
    "diff": "@@ -75,3 +78,16 @@ def test_empty_queryset(self):\n             with self.subTest(description), self.assertNumQueries(1):\n                 qs = Author.objects.annotate(annotation=Coalesce(empty_query, 42))\n                 self.assertEqual(qs.first().annotation, 42)\n+\n+    @skipUnless(\n+        connection.vendor == \"oracle\",\n+        \"Oracle specific test for Coalesce with JSONField\",\n+    )",
    "violations": [
      {
        "type": "",
        "line": 85,
        "review_comment": "Hello hello \ud83d\udc4b \nI couldn't fully investigate this as my local Oracle setup is not working (see https://forum.djangoproject.com/t/django-docker-box-oracle-ora-28001-the-account-has-expired-the-password-must-be-changed/44510)\nI like this direction and I confirmed this is passing on postgreql. I wanted to:\n- verify this test was failing before the changes and what error it failed with on Oracle\n- decide whether I think the current version of the test should continue to fail on Oracle (as it does with postgresql)\n- decide whether I think the test should belong in `tests/model_fields/test_jsonfield.py`\n\n@kanin-kearpimy you might be able to add your opinions here as well.\n(Note that the test name should be changed.)"
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/model_fields/models.py",
    "chunk_id": "PR20864_2935769204",
    "diff": "@@ -511,6 +511,11 @@ class AllFieldsModel(models.Model):\n     gr = GenericRelation(DataModel)\n \n \n+class MixedFieldsModel(models.Model):",
    "violations": [
      {
        "type": "",
        "line": 514,
        "review_comment": "Having a look around at the other models with a JSONField in this file, they seem to have a \"supports_json_field\" attribute in their `Meta`."
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/settings.py",
    "chunk_id": "PR20864_2936433127",
    "diff": "",
    "violations": [
      {
        "type": "",
        "line": 1,
        "review_comment": "Please can you remove this file?"
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/model_fields/models.py",
    "chunk_id": "PR20864_2936439673",
    "diff": "@@ -511,6 +511,11 @@ class AllFieldsModel(models.Model):\n     gr = GenericRelation(DataModel)\n \n \n+class MixedFieldsModel(models.Model):",
    "violations": [
      {
        "type": "",
        "line": 514,
        "review_comment": "I also think `MixedFieldsModel` beneath `AllFieldsModel` feels not clear/unspecific for how this is a model with a `JSONField` but with other fields as well\nI wonder how we feel abount adding nullable fields to `JSONModel` instead of creating a new model?"
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "django/db/models/functions/comparison.py",
    "chunk_id": "PR20864_2936460342",
    "diff": "@@ -86,9 +86,10 @@ def empty_result_set_value(self):\n         return None\n \n     def as_oracle(self, compiler, connection, **extra_context):\n-        # Oracle prohibits mixing TextField (NCLOB) and CharField (NVARCHAR2),\n+        # Oracle prohibits mixing TextField,\n+        # JSONField (NCLOB) and CharField (NVARCHAR2),\n         # so convert all fields to NCLOB when that type is expected.",
    "violations": [
      {
        "type": "",
        "line": 91,
        "review_comment": "```suggestion\n        # Oracle prohibits mixing TextField, JSONField (NCLOB) and CharField \n        # (NVARCHAR2), so convert all fields to NCLOB when that type is \n        # expected.\n```\n(nit) wrap comments at 79 characters"
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/settings.py",
    "chunk_id": "PR20864_2936807886",
    "diff": "",
    "violations": [
      {
        "type": "",
        "line": 1,
        "review_comment": "It's related with https://github.com/django/django-docker-box/issues/66."
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/model_fields/test_jsonfield.py",
    "chunk_id": "PR20864_2936809953",
    "diff": "@@ -1290,6 +1291,17 @@ def test_literal_annotation_filtering(self):\n         )\n         self.assertQuerySetEqual(qs, all_objects)\n \n+    def test_json_type_casting_with_coalesce(self):",
    "violations": [
      {
        "type": "",
        "line": 1294,
        "review_comment": "If you grep for \"supports_primitives_in_json_field\" in the codebase, you'll see that it's only False on Oracle versions below 21. We support down to version 19, and our Jenkins CI tests version 19. Django docker box tests on version 23."
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/settings.py",
    "chunk_id": "PR20864_2936814250",
    "diff": "",
    "violations": [
      {
        "type": "",
        "line": 1,
        "review_comment": "(If you don't see the same error about a file being written outside the mount on your setup, then perhaps you could open a separate issue on that repo that only concerns the fact that the tests/settings.py is not deleted at the conclusion of the test run.)"
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/model_fields/models.py",
    "chunk_id": "PR20864_2936819014",
    "diff": "@@ -452,6 +452,7 @@ class Meta:\n class RelatedJSONModel(models.Model):\n     value = models.JSONField()\n     json_model = models.ForeignKey(NullableJSONModel, models.CASCADE)\n+    summary = models.TextField(null=True, blank=True)",
    "violations": [
      {
        "type": "",
        "line": 455,
        "review_comment": "Optional suggestion: TextField is usually found with larger volumes of text, so I'd either change to `CharField` with a `max_length`, or rename to something other than \"summary\".\n\n(You can double-check me that your test after that alteration still fails on main.)"
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "scripts/tests.py",
    "chunk_id": "PR20854_2897506086",
    "diff": "@@ -0,0 +1,124 @@\n+#! /usr/bin/env python\n+\n+\"\"\"\n+Tests for scripts/prepare_commit_msg.py.",
    "violations": [
      {
        "type": "",
        "line": 4,
        "review_comment": "nitpick: this file is just \"tests.py\", so I'd remove the reference to the specific file being tested, e.g. place it in a class docstring instead."
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "django/contrib/staticfiles/storage.py",
    "chunk_id": "PR20852_2897421619",
    "diff": "@@ -276,12 +286,28 @@ def converter(matchobj):\n \n             # Determine the hashed name of the target file with the storage\n             # backend.\n-            hashed_url = self._url(\n-                self._stored_name,\n-                unquote(target_name),\n-                force=True,\n-                hashed_files=hashed_files,\n-            )\n+            try:\n+                hashed_url = self._url(\n+                    self._stored_name,\n+                    unquote(target_name),\n+                    force=True,\n+                    hashed_files=hashed_files,\n+                )\n+            except ValueError as exc:\n+                # raise substitution errors with collectstatic as CommandError\n+                error_message = (\n+                    \"{filename}' contains a URL '{matched}' on line {line}\\n\\n\"\n+                    \"The storage resolved the url to {missing} \"\n+                    \"and could not find that file.\"\n+                )\n+                raise CommandError(",
    "violations": [
      {
        "type": "",
        "line": 303,
        "review_comment": "I think only the command should re-raise a `CommandError`, not here. But we should definitely enrich the ValueError with whatever information we need. You can do that with [add_note()](https://docs.python.org/3/tutorial/errors.html#enriching-exceptions-with-notes)."
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "django/contrib/staticfiles/storage.py",
    "chunk_id": "PR20828_2885891179",
    "diff": "@@ -11,6 +11,12 @@\n from django.core.files.base import ContentFile\n from django.core.files.storage import FileSystemStorage, storages\n from django.utils.functional import LazyObject\n+from django.utils.regex_helper import _lazy_re_compile\n+\n+comment_re = _lazy_re_compile(r\"\\/\\*[^*]*\\*+([^/*][^*]*\\*+)*\\/\", re.DOTALL)\n+line_comment_re = _lazy_re_compile(\n+    r\"\\/\\*[^*]*\\*+([^/*][^*]*\\*+)*\\/|\\/\\/[^\\n]*\", re.DOTALL\n+)",
    "violations": [
      {
        "type": "",
        "line": 19,
        "review_comment": "I desperately need to look at #20096 next :-)"
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/logging_tests/tests.py",
    "chunk_id": "PR20792_2867391573",
    "diff": "@@ -882,3 +882,26 @@ def test_unicode_escape_escaping(self):\n                 )\n                 # Log record is always a single line.\n                 self.assertEqual(len(record.getMessage().splitlines()), 1)\n+\n+\n+class LoggingSignalTests(SimpleTestCase):",
    "violations": [
      {
        "type": "",
        "line": 887,
        "review_comment": "There is already a similar test class (`SetupConfigureLogging(SimpleTestCase)`) defined above. Rather than creating a separate class, it would be better to include this as an additional method in that existing class.\n\n```python\nclass SetupConfigureLogging(SimpleTestCase):\n    . . . .\n```"
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/logging_tests/tests.py",
    "chunk_id": "PR20792_2867391584",
    "diff": "@@ -882,3 +882,26 @@ def test_unicode_escape_escaping(self):\n                 )\n                 # Log record is always a single line.\n                 self.assertEqual(len(record.getMessage().splitlines()), 1)\n+\n+\n+class LoggingSignalTests(SimpleTestCase):\n+    def test_logging_setting_changed(self):\n+        \"\"\"\n+        The test client should reload logging configuration when the LOGGING\n+        setting is changed via @override_settings.\n+        \"\"\"\n+        new_logging = {\n+            \"version\": 1,\n+            \"disable_existing_loggers\": False,\n+            \"loggers\": {\n+                \"django.test_custom_logger\": {\n+                    \"level\": \"WARNING\",\n+                }\n+            },\n+        }\n+\n+        with override_settings(LOGGING=new_logging):\n+            import logging",
    "violations": [
      {
        "type": "",
        "line": 904,
        "review_comment": "```suggestion\n```\n`import logging` is already present at the top of the file, so there is no need to import it again inside the override_settings"
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/logging_tests/tests.py",
    "chunk_id": "PR20792_2867392675",
    "diff": "@@ -882,3 +882,26 @@ def test_unicode_escape_escaping(self):\n                 )\n                 # Log record is always a single line.\n                 self.assertEqual(len(record.getMessage().splitlines()), 1)\n+\n+\n+class LoggingSignalTests(SimpleTestCase):\n+    def test_logging_setting_changed(self):\n+        \"\"\"\n+        The test client should reload logging configuration when the LOGGING\n+        setting is changed via @override_settings.",
    "violations": [
      {
        "type": "",
        "line": 891,
        "review_comment": "The docstring states, \u201cThe test client should reload logging configuration,\u201d but this is not related to the test client (`django.test.Client`). The implementation is actually about the `setting_changed` signal handler in response to `override_settings`. Therefore, it seems that the description should be revised to better reflect the actual behavior."
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/logging_tests/tests.py",
    "chunk_id": "PR20792_2867397001",
    "diff": "@@ -882,3 +882,26 @@ def test_unicode_escape_escaping(self):\n                 )\n                 # Log record is always a single line.\n                 self.assertEqual(len(record.getMessage().splitlines()), 1)\n+\n+\n+class LoggingSignalTests(SimpleTestCase):\n+    def test_logging_setting_changed(self):\n+        \"\"\"\n+        The test client should reload logging configuration when the LOGGING\n+        setting is changed via @override_settings.\n+        \"\"\"\n+        new_logging = {\n+            \"version\": 1,\n+            \"disable_existing_loggers\": False,\n+            \"loggers\": {\n+                \"django.test_custom_logger\": {\n+                    \"level\": \"WARNING\",\n+                }\n+            },\n+        }\n+\n+        with override_settings(LOGGING=new_logging):\n+            import logging\n+\n+            logger = logging.getLogger(\"django.test_custom_logger\")\n+            self.assertEqual(logger.level, logging.WARNING)",
    "violations": [
      {
        "type": "",
        "line": 907,
        "review_comment": "It does verify that logging is reconfigured, but it does not check whether the configuration is restored when exiting the with block. Since the setting_changed signal also fires on exit, it would be better to verify the restoration as well.\r\n\r\n```python\r\noriginal_level = logger.level\r\nwith override_settings(LOGGING=new_logging):\r\n\t\tlogger = logging.getLogger(\"django.test_custom_logger\")\r\n\t\tself.assertEqual(logger.level, logging.WARNING)\r\nself.assertEqual(logger.level, original_level)\r\n```"
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "django/test/signals.py",
    "chunk_id": "PR20792_2879312307",
    "diff": "@@ -251,3 +252,11 @@ def user_model_swapped(*, setting, **kwargs):\n             from django.contrib.auth import views\n \n             views.UserModel = UserModel\n+\n+\n+@receiver(setting_changed)\n+def update_logging_config(*, setting, **kwargs):\n+    if setting in {\"LOGGING\", \"LOGGING_CONFIG\"}:\n+        from django.conf import settings",
    "violations": [
      {
        "type": "",
        "line": 260,
        "review_comment": "Question: Why import settings here rather than in the file?"
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/logging_tests/tests.py",
    "chunk_id": "PR20792_2879342957",
    "diff": "@@ -586,6 +586,31 @@ def test_configure_initializes_logging(self):\n             setup()\n         self.assertTrue(dictConfig.called)\n \n+    def test_logging_settings_changed(self):\n+        \"\"\"\n+        Logging configuration should be reconfigured when LOGGING or\n+        LOGGING_CONFIG settings are changed.\n+        \"\"\"\n+        new_logging = {\n+            \"version\": 1,\n+            \"disable_existing_loggers\": False,\n+            \"loggers\": {\n+                \"django.test_custom_logger\": {\n+                    \"level\": \"WARNING\",\n+                }\n+            },\n+        }\n+        logger = logging.getLogger(\"django.test_custom_logger\")\n+        original_level = logger.level",
    "violations": [
      {
        "type": "",
        "line": 604,
        "review_comment": "Suggestion: If the default level is \"WARNING\", this test would still pass. Perhaps worth updating the config twice - once sets it to INFO, then assert, followed by a second set to WARNING, with a follow-up assert. That way it won't accidentally regress."
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/logging_tests/tests.py",
    "chunk_id": "PR20792_2902457813",
    "diff": "@@ -586,6 +586,31 @@ def test_configure_initializes_logging(self):\n             setup()\n         self.assertTrue(dictConfig.called)\n \n+    def test_logging_settings_changed(self):\n+        \"\"\"\n+        Logging configuration should be reconfigured when LOGGING or\n+        LOGGING_CONFIG settings are changed.\n+        \"\"\"\n+        new_logging = {\n+            \"version\": 1,\n+            \"disable_existing_loggers\": False,\n+            \"loggers\": {\n+                \"django.test_custom_logger\": {\n+                    \"level\": \"WARNING\",\n+                }\n+            },\n+        }\n+        logger = logging.getLogger(\"django.test_custom_logger\")\n+        original_level = logger.level",
    "violations": [
      {
        "type": "",
        "line": 604,
        "review_comment": "Rather than use `original_level`, assert that it's a specific value."
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "django/utils/deprecation.py",
    "chunk_id": "PR20789_2866389733",
    "diff": "@@ -17,6 +17,8 @@ def django_file_prefixes():\n         file = django.__file__\n     except AttributeError:\n         return ()\n+    if file is None:\n+        return ()",
    "violations": [
      {
        "type": "",
        "line": 18,
        "review_comment": "Interesting, I guess this means that these environements will have _leaky_ warnings (pointing at Django internals) but that's not a huge deal."
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "django/utils/deprecation.py",
    "chunk_id": "PR20789_2873491852",
    "diff": "@@ -17,6 +17,8 @@ def django_file_prefixes():\n         file = django.__file__\n     except AttributeError:\n         return ()\n+    if file is None:\n+        return ()",
    "violations": [
      {
        "type": "",
        "line": 21,
        "review_comment": "I would rather we do something like this:\n```suggestion\n    file = getattr(django, \"__file__\", None)\n    if file is None:\n        return ()\n```\nIf you don't object, I can push this tiny change during the afternoon."
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/backends/sqlite/test_creation.py",
    "chunk_id": "PR20768_2855631105",
    "diff": "@@ -41,3 +42,55 @@ def test_get_test_db_clone_settings_not_supported(self, *mocked_objects):\n         msg = \"Cloning with start method 'unsupported' is not supported.\"\n         with self.assertRaisesMessage(NotSupportedError, msg):\n             connection.creation.get_test_db_clone_settings(1)\n+\n+    @unittest.skipUnless(\n+        multiprocessing.get_start_method() in {\"forkserver\", \"spawn\"},\n+        \"Requires forkserver or spawn start method.\",\n+    )",
    "violations": [
      {
        "type": "",
        "line": 49,
        "review_comment": "Actually we do that elsewhere, I think it's fine:\nhttps://github.com/django/django/blob/4aefc9ea51cc2d78f43b1dc2aa69732e55d18a56/tests/test_runner/test_discover_runner.py#L48"
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/backends/sqlite/test_creation.py",
    "chunk_id": "PR20768_2855641488",
    "diff": "@@ -41,3 +42,55 @@ def test_get_test_db_clone_settings_not_supported(self, *mocked_objects):\n         msg = \"Cloning with start method 'unsupported' is not supported.\"\n         with self.assertRaisesMessage(NotSupportedError, msg):\n             connection.creation.get_test_db_clone_settings(1)\n+\n+    @unittest.skipUnless(\n+        multiprocessing.get_start_method() in {\"forkserver\", \"spawn\"},\n+        \"Requires forkserver or spawn start method.\",\n+    )\n+    def test_setup_worker_connection_respects_test_database_name(self, *mocked_objects):\n+        test_connection = copy.copy(connections[DEFAULT_DB_ALIAS])\n+        test_connection.settings_dict = copy.deepcopy(\n+            connections[DEFAULT_DB_ALIAS].settings_dict\n+        )\n+        tests = [\n+            (\"mytest.db\", \"mytest_2.db\"),\n+            (\"mytest\", \"mytest_2\"),\n+        ]\n+        for test_db_name, expected_source_db_name in tests:\n+            with self.subTest(test_db_name=test_db_name):\n+                # When calling setup_worker_connection(), the test database has been\n+                # created already and its name has been copied to settings_dict[\"NAME\"],\n+                # so we don't need to set settings_dict[\"TEST\"][\"NAME\"] here.",
    "violations": [
      {
        "type": "",
        "line": 63,
        "review_comment": "Good call, I feel it's a better debugging experience if the test manipulates what's more relevant."
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "django/core/handlers/asgi.py",
    "chunk_id": "PR20749_2855463736",
    "diff": "@@ -54,8 +54,14 @@ def __init__(self, scope, body_file):\n         self.path = scope[\"path\"]\n         self.script_name = get_script_prefix(scope)\n         if self.script_name:\n-            # TODO: Better is-prefix checking, slash handling?\n-            self.path_info = scope[\"path\"].removeprefix(self.script_name)\n+            # Ensure script_name is a proper path-segment prefix.\n+            if (\n+                scope[\"path\"].startswith(self.script_name + \"/\")",
    "violations": [
      {
        "type": "",
        "line": 59,
        "review_comment": "What if script_name ends with a slash?"
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "django/core/handlers/asgi.py",
    "chunk_id": "PR20749_2898109483",
    "diff": "@@ -54,10 +54,13 @@ def __init__(self, scope, body_file):\n         self.path = scope[\"path\"]\n         self.script_name = get_script_prefix(scope)\n         if self.script_name:\n-            # TODO: Better is-prefix checking, slash handling?\n-            self.path_info = scope[\"path\"].removeprefix(self.script_name)\n+            script_name = self.script_name.rstrip(\"/\")\n+            if self.path.startswith(script_name + \"/\") or self.path == script_name:\n+                self.path_info = self.path[len(script_name) :]\n+            else:\n+                self.path_info = self.path\n         else:\n-            self.path_info = scope[\"path\"]\n+            self.path_info = self.path",
    "violations": [
      {
        "type": "",
        "line": 63,
        "review_comment": "This looks a little unrelated, but because I used `self.path` above to avoid splitting over multiple lines, I had to be consistent."
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/admin_views/test_password_form.py",
    "chunk_id": "PR20735_2834253314",
    "diff": "@@ -142,3 +143,16 @@ def test_change_password_for_existing_user(self):\n         # Only the set password submit button is visible.\n         self.assertIs(submit_set.is_displayed(), True)\n         self.assertIs(submit_unset.is_displayed(), False)\n+\n+    @screenshot_cases([\"desktop_size\", \"mobile_size\", \"rtl\", \"dark\", \"high_contrast\"])\n+    def test_add_user_alignment(self):\n+        \"\"\"\n+        Ensures that radio buttons and labels are properly aligned. A CSS\n+        regression on .wide legend would produce a visibly misaligned\n+        screenshot.\n+        \"\"\"",
    "violations": [
      {
        "type": "",
        "line": 153,
        "review_comment": "Please reword to avoid preambles like \"ensures that\". The test title should probably mention radio button field alignment, not the add user view used in the test.."
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/admin_views/test_password_form.py",
    "chunk_id": "PR20735_2834253829",
    "diff": "@@ -142,3 +143,16 @@ def test_change_password_for_existing_user(self):\n         # Only the set password submit button is visible.\n         self.assertIs(submit_set.is_displayed(), True)\n         self.assertIs(submit_unset.is_displayed(), False)\n+\n+    @screenshot_cases([\"desktop_size\", \"mobile_size\", \"rtl\", \"dark\", \"high_contrast\"])\n+    def test_add_user_alignment(self):\n+        \"\"\"\n+        Ensures that radio buttons and labels are properly aligned. A CSS\n+        regression on .wide legend would produce a visibly misaligned\n+        screenshot.\n+        \"\"\"\n+        user_add_url = reverse(\"auth_test_admin:auth_user_add\")\n+        self.admin_login(username=\"super\", password=\"secret\")\n+        self.selenium.get(self.live_server_url + user_add_url)\n+\n+        self.take_screenshot(\"add_user\")",
    "violations": [
      {
        "type": "",
        "line": 158,
        "review_comment": "linter failure here."
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/test_client/tests.py",
    "chunk_id": "PR20733_2829426924",
    "diff": "@@ -814,7 +814,20 @@ def test_view_with_permissions(self):\n             response, \"/accounts/login/?next=/permission_protected_view/\"\n         )\n \n-        # TODO: Log in with right permissions and request the page again\n+        # Get the REAL permission using standard Django ORM\n+        permission = Permission.objects.get(\n+            content_type__app_label=\"auth\", codename=\"add_user\"\n+        )\n+        self.u1.user_permissions.add(permission)\n+\n+        # Log in to refresh the session with the new permission\n+        self.client.login(username=\"testclient\", password=\"password\")\n+",
    "violations": [
      {
        "type": "",
        "line": 825,
        "review_comment": "Permissions are not stored in the session but checked at run time. I suspect you don't need to log in again at all here.\n\n\n```suggestion\n```"
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/test_client/tests.py",
    "chunk_id": "PR20733_2829428431",
    "diff": "@@ -853,7 +866,20 @@ def test_view_with_method_permissions(self):\n             response, \"/accounts/login/?next=/permission_protected_method_view/\"\n         )\n \n-        # TODO: Log in with right permissions and request the page again\n+        # Get the REAL permission using standard Django ORM\n+        permission = Permission.objects.get(\n+            content_type__app_label=\"auth\", codename=\"add_user\"\n+        )\n+        self.u1.user_permissions.add(permission)\n+\n+        # Log in to refresh the session with the new permission\n+        self.client.login(username=\"testclient\", password=\"password\")",
    "violations": [
      {
        "type": "",
        "line": 876,
        "review_comment": "Ditto\n\n\n```suggestion\n```"
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/migrations/test_loader.py",
    "chunk_id": "PR20728_2851692876",
    "diff": "@@ -698,7 +698,6 @@ def test_loading_order_does_not_create_circular_dependency(self):\n             test_settings.write(f\"INSTALLED_APPS += {INSTALLED_APPS}\\n\")\n \n         test_environ = os.environ.copy()\n-        test_environ[\"PYTHONPATH\"] = str(tests_dir)",
    "violations": [
      {
        "type": "",
        "line": 701,
        "review_comment": "I'll try to look into this ticket this week. I didn't think there were any failing tests last time I checked, and it's weird that Docker is behaving differently on Windows... \ud83e\udd14\r\n\r\n**Edit:** Oh. Much of this is since upgrading to use Python 3.14 which introduced coloured output to `argparse`. Will look into the other issues raised though."
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/migrations/test_loader.py",
    "chunk_id": "PR20728_2855483563",
    "diff": "@@ -698,7 +698,6 @@ def test_loading_order_does_not_create_circular_dependency(self):\n             test_settings.write(f\"INSTALLED_APPS += {INSTALLED_APPS}\\n\")\n \n         test_environ = os.environ.copy()\n-        test_environ[\"PYTHONPATH\"] = str(tests_dir)",
    "violations": [
      {
        "type": "",
        "line": 701,
        "review_comment": "Nice, appending to the path instead of overwriting seems much saner \ud83d\ude48 !"
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/migrations/test_loader.py",
    "chunk_id": "PR20728_2857712509",
    "diff": "@@ -698,7 +698,6 @@ def test_loading_order_does_not_create_circular_dependency(self):\n             test_settings.write(f\"INSTALLED_APPS += {INSTALLED_APPS}\\n\")\n \n         test_environ = os.environ.copy()\n-        test_environ[\"PYTHONPATH\"] = str(tests_dir)",
    "violations": [
      {
        "type": "",
        "line": 701,
        "review_comment": "Ah, nice. Good fix \ud83d\ude04"
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/tasks/test_immediate_backend.py",
    "chunk_id": "PR20722_2854334692",
    "diff": "@@ -228,6 +228,15 @@ def test_failed_logs(self):\n         self.assertIn(\"state=FAILED\", captured_logs.output[2])\n         self.assertIn(result.id, captured_logs.output[2])\n \n+    def test_successful_task_no_none_type_in_logs(self):\n+        with self.assertLogs(\"django.tasks\", level=\"DEBUG\") as captured_logs:\n+            result = test_tasks.noop_task.enqueue()\n+\n+        self.assertEqual(result.status, TaskResultStatus.SUCCESSFUL)\n+\n+        for log_output in captured_logs.output:\n+            self.assertNotIn(\"NoneType\", log_output)\n+",
    "violations": [
      {
        "type": "",
        "line": 239,
        "review_comment": "Nit: Perhaps make the check more vague, to catch other cases something gets accidentally logs as `None`\n```suggestion\n    def test_successful_task_no_none_in_logs(self):\n        with self.assertLogs(\"django.tasks\", level=\"DEBUG\") as captured_logs:\n            result = test_tasks.noop_task.enqueue()\n\n        self.assertEqual(result.status, TaskResultStatus.SUCCESSFUL)\n\n        for log_output in captured_logs.output:\n            self.assertNotIn(\"None\", log_output)\n\n```"
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/tasks/test_immediate_backend.py",
    "chunk_id": "PR20722_2854362321",
    "diff": "@@ -228,6 +228,15 @@ def test_failed_logs(self):\n         self.assertIn(\"state=FAILED\", captured_logs.output[2])\n         self.assertIn(result.id, captured_logs.output[2])\n \n+    def test_successful_task_no_none_type_in_logs(self):\n+        with self.assertLogs(\"django.tasks\", level=\"DEBUG\") as captured_logs:\n+            result = test_tasks.noop_task.enqueue()\n+\n+        self.assertEqual(result.status, TaskResultStatus.SUCCESSFUL)\n+\n+        for log_output in captured_logs.output:\n+            self.assertNotIn(\"NoneType\", log_output)\n+",
    "violations": [
      {
        "type": "",
        "line": 239,
        "review_comment": "I can fold that in. Let's hope the string \"None\" never ends up in the task id \ud83d\ude09 "
      }
    ]
  },
  {
    "repo": "django/django",
    "file": "tests/gis_tests/geos_tests/test_io.py",
    "chunk_id": "PR20720_2817926242",
    "diff": "@@ -52,10 +51,8 @@ def test02_wktwriter(self):\n     def test_wktwriter_constructor_arguments(self):\n         wkt_w = WKTWriter(dim=3, trim=True, precision=3)\n         ref = GEOSGeometry(\"POINT (5.34562 23 1.5)\")\n-        if geos_version_tuple() > (3, 10):\n-            ref_wkt = \"POINT Z (5.346 23 1.5)\"\n-        else:\n-            ref_wkt = \"POINT Z (5.35 23 1.5)\"\n+",
    "violations": [
      {
        "type": "",
        "line": 54,
        "review_comment": "No blank line needed."
      }
    ]
  }
]